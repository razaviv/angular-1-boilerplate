app.directive("tabsComponent", ($rootScope) => {
  return {
    templateUrl: "/screens/dashboard/components/tabs/tabs.html",
    scope: true,
    link: function(scope, element, attrs) {

      // code

    }
  }
});
